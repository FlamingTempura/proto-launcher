{
  "name": "launcher",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron . --enable-logging",
    "build": "electron-packager . --out bin --ignore bin --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "electron": "^3.0.3",
    "electron-packager": "^12.2.0"
  }
}
